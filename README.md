# One Tape, Same Machine — corrected

در `single_tape.html`:
- فقط یک نوار فیزیکی A#B وجود دارد.
- هر خانه فقط یک محتوای قابل مشاهده دارد: `0`، `1`، `#` یا `HEAD`.
- هیچ `HEAD A` یا `HEAD B` داخل خانه نوشته نمی‌شود.
- HEADهای منطقی A و B خودِ خانه‌های نوار هستند و با حرکتشان، خانه قبلی نماد اصلی را پس می‌دهد.
- هد فیزیکی یک هد واحد است که بالای نوار دیده می‌شود و خانه‌به‌خانه حرکت می‌کند.
- موقعیت هد فیزیکی با دورخط همان خانه نیز مشخص می‌شود؛ محتوای خانه را عوض نمی‌کند.
- الگوریتم کاملاً خودکار اجرا می‌شود.
